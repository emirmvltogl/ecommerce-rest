spring.application.name=demo
spring.datasource.url=jdbc:mysql://localhost:3306/${DATABASE_URL}
spring.datasource.username=${DATABASE_NAME}
spring.datasource.password=${DATABASE_PASSWORD}

###SHOW LOGGİNG###

logging.level.org.springframework.security=DEBUG
### UPDATE TABLE###
spring.jpa.hibernate.ddl-UPDATE

# Hibernate sorgularını göster
# Hibernate ayarları
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true


### MAİL SENDER PROPERTİES #
spring.mail.host=${MAIL_SMPT}
spring.mail.port=${MAIL_PORT}
spring.mail.username=${EMAIL}
spring.mail.password=${EMAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


jwt.secret=${jwt_secret}


logging.level.org.springframework.security=DEBUG
frontend.url=${frontend-url}

logging.level.org.springframework.security.authentication.ProviderManager=DEBUG
